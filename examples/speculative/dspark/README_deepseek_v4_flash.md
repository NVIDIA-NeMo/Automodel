# Training a DeepSeek-V4-Flash DSpark drafter

DSpark draft training for a frozen **DeepSeek-V4-Flash** target, using the same
semi-autoregressive method and three-term objective as the Qwen3 / Gemma4 DSpark
configs. The V4 target is loaded frozen via the V4 finetune recipe's distributed
path (expert-parallel + FSDP2): the 256 routed experts shard across ranks and the
FP8/FP4 base weights dequantize to bf16 on load. The draft is a small dense
V4-attention stack and hidden states are captured online (no offline cache).

## Prerequisites

- **DeepEP** (or HybridEP) installed. The V4 MoE token dispatcher requires it;
  without it `setup()` raises `ImportError: HybridEP is not installed`.
- 8 GPUs, >= 80 GiB each. V4-Flash is ~530 GiB in bf16, ~66 GiB/rank at `ep_size=8`.
- A local DeepSeek-V4-Flash checkpoint (or the hub id `deepseek-ai/DeepSeek-V4-Flash`).

## 1. Prepare data

DSpark is teacher-forced, so regenerate the responses with the **target** first
to avoid a train/inference distribution mismatch (as in the DSpark paper). Take
Open-PerfectBlend prompts, generate responses with V4-Flash, and write
OpenAI-format `messages` rows to a jsonl:

```json
{"messages": [{"role": "user", "content": "..."}, {"role": "assistant", "content": "<response regenerated by V4-Flash>"}]}
```

Point `recipe_args.train_data_path` at that jsonl.

## 2. Smoke test (run this first)

A fast pipeline-sanity run of the real EP path, shrunk to reach a training step in
minutes. It confirms the distributed target loads, hidden capture works across the
shards, the draft trains, and the three-term loss is finite and trends down:

```
TARGET=/path/to/DeepSeek-V4-Flash bash tests/functional_tests/speculative/run_deepseek_v4_flash_smoke.sh
```

It runs the CPU unit tests, builds 64 tiny smoke rows, runs an 8-GPU training
smoke with `deepseek_v4_flash_smoke.yaml`, and asserts a finite, non-increasing
loss.

## 3. Full training

```
torchrun --standalone --nproc_per_node=8 -m nemo_automodel.recipes.llm.train_dspark -c examples/speculative/dspark/deepseek_v4_flash_dspark.yaml
```

Set `target_model_name_or_path` and `train_data_path` in the yaml first. Per-step
metrics (`loss`, `ce_loss`, `l1_loss`, `confidence_loss`, `lr`, `mem`) are written
to `<output_dir>/dspark_train_metrics.jsonl`.

## Validation status

- **Verified**: CPU unit tests for the draft / config / registry / RoPE `inv_freq`
  fp32 pin (`tests/unit_tests/speculative/test_dspark_draft_deepseek_v4.py`).
- **Needs a first run on a DeepEP-equipped box** (could not be validated in an
  environment without DeepEP): the full distributed target load and hidden-state
  capture across the EP/FSDP shards. When you run the smoke, eyeball these 5
  points:
  1. FP8/FP4 base weights dequantize to bf16 on load (target forward stays finite).
  2. Captured hidden states are full per-rank (not a sharded DTensor that needs an all-gather).
  3. The frozen V4 target forward runs with a 2D mask and `use_cache=False`.
  4. `ep_size` divides `n_routed_experts` (256); the default 8 gives 32 experts/rank.
  5. Per-rank weight memory is ~66 GiB (watch `nvidia-smi` during the run).
