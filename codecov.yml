comment: false
coverage:
  status:
    project: false
    patch:
      default:
        informational: true
        target: 80%
        base: auto
        if_ci_failed: error
        if_not_found: success

fixes:
  - "/workspace/::"
